type ActiveListingID @entity {
  id: ID!
  listingId: ID!
}

enum ListingStatus { Created Active Cancelled Finished }

type Listing @entity {
  id: ID!
  status: ListingStatus!

  nft: Bytes!
  nftId: BigInt!

  author: Bytes!

  bidder: Bytes
  bid: BigInt

  endTimestamp: BigInt!
}
