type ActiveListingID @entity {
  id: ID!
  listingId: ID!
}

type Listing @entity {
  id: ID!

  nft: Bytes!
  nftId: BigInt!

  author: Bytes!

  bidder: Bytes
  bid: BigInt

  endTimestamp: BigInt!
}
